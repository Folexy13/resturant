import 'reflect-metadata';

// This file is used for Jest setup configuration
// The actual test database setup is done in individual test files

describe('Test Setup', () => {
  it('should have reflect-metadata loaded', () => {
    expect(true).toBe(true);
  });
});